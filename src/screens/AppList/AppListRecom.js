import React from "react"
import { Text, SafeAreaView } from "react-native"

const AppListRecom = () => {
    return (
        <SafeAreaView style={{ backgroundColor: "#141414", flex: 1 }}>
            <Text>{`AppListRecom`}</Text>
        </SafeAreaView>
    )
}

export default AppListRecom